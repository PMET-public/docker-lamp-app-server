; ============== Common configuration.

${php_common_config}

; ============== Configuration for the CLI SAPI.

; Limits.
max_execution_time=0
max_input_time=-1
memory_limit=-1

% for comment, config in php_extensions:
; =============== ${comment}
${config}
% endfor

% if php_custom_config:
; =============== Custom configuration.
${php_custom_config}
% endif
